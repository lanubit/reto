import {RepositoryTypes} from "./types/repository.type";
import {ServiceTypes} from "./types/service.type";

export const TYPES = {
    Repositories: RepositoryTypes,
    Services: ServiceTypes,
};

export default TYPES;

/*const TAGS = {};

export { TYPES, TAGS };*/